using DioReservaHotel.Utils

namespace DioReservaHotel {

class Program
{
    static void Main(string[] args)
    {
        // ... (código para criar uma lista de suítes, por exemplo)

        while (true)
        {
            // ... (menu de opções: fazer reserva, listar reservas, sair)

            switch (opcao)
            {
                case 1:
                    // Solicitar dados do hóspede e da suíte
                    // Criar um objeto Reserva e adicionar à lista de reservas
                    break;
                // ... outros casos
            }
        }
    }
}
}
